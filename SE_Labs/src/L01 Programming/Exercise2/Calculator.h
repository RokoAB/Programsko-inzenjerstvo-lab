//
//	Calculator.h
//
//  © 2024 Zoraja Consulting. All rights reserved but even though use it.

#pragma once

#include <string>

namespace math
{
	class Calcuator
	{
	public:
		static float Add(float = 0.0f, float = 0.0f);
		static float Sub(float = 0.0f, float = 0.0f);
		static float Mul(float = 0.0f, float = 0.0f);
		static float Div(float = 0.0f, float = 1.0f);
	};
}